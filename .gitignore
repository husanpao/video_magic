# ── 用户数据：**绝不提交** ──────────────────────────────────────────────────
# projects/ 下是每一部片子的正文、镜头表、定妆照、视频、成片 —— 体积大且是你自己的内容。
# clone 下来是一个**空跑得起来的工具**，不是一个带着别人素材的仓库。
projects/

# 手工备份（含抢救出来的镜头表）
_backup/

# ── 构建产物：可再生 ────────────────────────────────────────────────────────
# 前端构建产物由 `cd web && npm run build:only` 生成到 vm/static/dist/
vm/static/dist/

# ── 依赖 ────────────────────────────────────────────────────────────────────
node_modules/

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 运行期产物 ──────────────────────────────────────────────────────────────
*.log
*.out
*.tmp
*.bak
.DS_Store

# ── 本地调研草稿（正式报告在 research/*.md）────────────────────────────────
_research/

# ── 密钥：**绝不提交** ──────────────────────────────────────────────────────
# DeepSeek key 默认读 ~/.config/video_magic/deepseek_key，但别处也可能散落副本
*key*
*secret*
!**/keywords*.py
.env
.env.*

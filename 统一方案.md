# 统一处理方案（5 份调研 + 本机节点包发现）

> 2026-09-23 · 输入：5 份外部调研（Director-WebUI / Slate+MCWW+acqm+storyboard-master / 生态全景 106 项目 / UI-UX printfilm+milimo / storyforge+wind-comic）+ 本机 T8 节点包能力清点
> 原始条目：`NEXT-ACTIONS.md`（351 行 / 81 项）
> 报告：`Director-WebUI-调研报告.md`、`research/reports/07*.md`、`research/reports/08*.md`、`AI短剧漫剧开源项目全景图谱.md`、`research/novel-to-drama-开源调研.md`

---

## 零、三条最重要的判断

### 判断 1：钱不该花在"更便宜地出片"，该花在"减少废片"

wind-comic 的 README 里有一组行业数据（调研者认为比它所有技术细节都重要）：

> 「**制作只占总成本 7.5%，投流占 70-85%**……这意味着『把制作成本再砍一半』对项目盈亏几乎无影响 —— 任何以『更便宜地出片』为唯一卖点的工具，**价值天花板极低**」
> 「AI 漫剧爆款率不足 **0.1%**、约 **90%** 公司亏损」「平台正给纯 AI 生成降权」

**→ 结论：把工程资源投向审计/质检，而不是省 GPU 时间。** 这一条直接把"渲染前置质检"提到最高优先级（下面 **B 档**），并把"units 聚合省时间"的排序下调（降到 **C 档**，它的主要价值是**连续性**而非省时间）。

### 判断 2：我们在两个"被全行业低估"的点上已经领先

| 能力 | 行业覆盖（端到端类 18 项目） | 我们 |
| --- | --- | --- |
| **产物幂等指纹 + stale 清单** | **6/18**（最被低估） | ✅ 已有 |
| **换装 / 多形态** | **4/18**（最被低估） | ✅ 刚做完 |
| **小说原文对白零丢失** | **两个深挖项目都没做到** | ✅ 已有（29 句逐字比对，实测 0 丢失） |

- storyforge 的原文在**第 1 步**就被截断到 12K 字符 + LLM 摘要，此后全程只基于摘要工作，**它的 `dialogue` 是 LLM 重新创作**（`grep dialogue backend/` 证实全程不引用原文）——**架构级错误，走错很难补**
- wind-comic 只对"已是剧本格式"的输入靠正则做到了原文保留
- **→ 我们不该在这里"补课"，而该把它做成卖点并加固**（见 B5）

### 判断 3：本机已有整套长片能力，我们一个都没用

`minimax_h3_fl2va_*` 的 **FL2VA = First-Last frame to Video+Audio**，我们却只用了 Ref2V 模式。本机 T8 节点包里有一整套长片编排、latent 检查点、接缝漂移修复、音色库、台词边界 ASR（详见 **C 档**）。**这比任何外部项目都更直接可用。**

---

## A 档 · 正确性隐患（必须先修，全部属于"静默失效"）

| # | 项 | 后果 | 依据 |
| --- | --- | --- | --- |
| **A1** | **FL2VA/T2VA 段落结构错误**：`gen.py` 的 no-ref 分支（空镜走 `MiniMaxH3AudioConditioningT8`）传的是**六段式**，而 FL2VA 系应按**三段式**（`integrated_multimodal_description` / `overall_soundscape` / `non_diegetic_music`） | 本机 52 镜无空镜所以未触发，但**一旦有空镜就出错** | Director-WebUI `timelineProject.ts:531-543` + 测试显式断言 `not.toContain("subject_definitions:")` |
| **A2** | **concat 必须用 `sec_actual`（ffprobe 回读）**：H3 有 `frames % 17` 步进，**请求 6s 实出 5.96/6.04s** | 现在按请求值算时间轴 → **拼 52 镜后字幕累积漂移** | Director-WebUI 三层时长 + `comfyui_h3.py:20-23` |
| **A3** | **"能力不满足导致的降级"必须在 manifest 显式留行** | 隐蔽坑：请求 30s 但引擎上限 <30s ⇒ 链空 ⇒ 上游拼出误导性的「no providers」⇒ **静默降级出 10s 片，决策日志上没有任何一行说时长被降过**。本地显存会直接限制时长，**这个坑我们一定会遇到** | wind-comic `diagnose_empty_chain()` |
| **A4** | **台词原文不进主 `visualPrompt`**（仅原生音频 provider 读） | 防「**AI 把台词画成乱码字**」的**结构性解法**，比只在负面词加 `text` 可靠 | wind-comic `spokenDialogue` 分离 |
| **A5** | **Autogrow 稠密槽位 fail-closed**：`ref_images.ref_image_N` 出现稀疏槽位时**直接报错，绝不静默重编号** | Autogrow 展示序 = 提示词编号 → **服装变体删中间图必踩** | Director-WebUI `native_templates.py:1250-1259` |
| **A6** | **排队宽限**：超时先查 `/queue` 位置，仍在队列就宽限；只有"不在队列也无 history"才判丢失 | 防"旧 prompt 迟到入队"→ 误判丢失 → **重复提交** | Slate `comfyui_client.py:374-385` + MCWW `queueing.py:267-285` |
| **A7** | **LoRA 按文件名路由要核实**：`*_T8`(4/8step) → `LoraLoaderBypassModelOnly`；`*_comfyui_bf16` → `LoraLoaderModelOnly`。我们的 `minimax_h3_fl2v_turbo_8step_v1.0_comfyui_bf16` **同时含两种特征** | 路由错会**影响画质** | Director-WebUI `docs/native-workflow-execution.md:155-162` |
| **A8** | **取消用两段式**：先 `POST /queue {delete:[pid]}` 再 `/interrupt` | 比单发 interrupt 精确 | MCWW `comfyAPI.py:60-96` |

---

## B 档 · 零 LLM 审计层（**最高性价比，直击"废片率"**）

**共同特征：只需已有的分镜表 JSON，零 token、零 GPU，且是渲染前的质检。**

| # | 项 | 为什么值钱 |
| --- | --- | --- |
| **B1** | **对话覆盖度审计** | 检测两类病：**多角色对话只有 1 镜**（缺正反打）、**≥2 镜但全 wide 无特写**。作者自述：**「漫剧约 50% 镜头是对话场景……一段连续对话用 1 个 wide shot 涵盖，显得非常『AI 一遍跑完』。这是漫剧『AI 感』最大来源之一」** |
| **B2** | **节奏/冲突形状审计** | `analyzeConflictShape`（最小二乘斜率 + 峰值显著度）、`findDragSegments`（**连续 ≥3 镜低于阈值**）、`auditOpening`（前 1/3 密度）、`auditDurationRhythm`（**变异系数 `cv<0.12` → 呆板**）。动机：**「平均分会把『高开低走』和『层层递进』算成同一个数」**（`[1,2,4,9]` 与 `[9,4,2,1]` 平均都是 4）；「报『平均 5.2 分』没用，要指出**第几到第几镜是观众划走的地方**」 |
| **B3** | **逐镜 7 维完备性矩阵 + blockers** | `{角色参考图, 场景参考图, 分镜图, 视频片段, 台词, 配音, 配乐} × {missing\|draft\|approved}`。比"第 5 镜有问题"信息密度高得多 —— 能说「**第 5 镜的角色参考图还只是 draft**」 |
| **B4** | **时长守恒校验** | micro-beat 的「2-4 条、每条 2-5s、**时长之和 = duration**」要**确定性校验 `abs(sum-sec)<0.05`**，不满足则本地缩放或带错重出 —— **别只靠 prompt** |
| **B5** | **把"原文对白零丢失"加固成量化门禁** | 我们已有检查，升级为：分镜表加 `dialogue: [{char, src_span:[start,end], quote}]`，**确定性校验 `chapter_text[a:b] == quote`（纯字符串比对，零成本）** + 覆盖率审计（「原文 47 句，采纳 44 句，遗漏 3 句」）→ **可进 manifest、可做 CI 门禁**。这是两个深挖项目都没做到的事 |
| **B6** | **情绪 → TTS 韵律确定性映射** | 16 条中文情绪正则 → 基线 `(speed,pitch,vol)`；**让 DeepSeek 拆镜时顺带输出 `emotion` + `emotion_temperature`（复用同一次调用，零额外 token）**，一路传到 TTS，实现"画面走情绪，配音跟着走" |
| **B7** | **工程规范层**（storyforge `docs/MODEL_POLICY.md`） | 分级 TTL 缓存 + 内容寻址 key（`X-Bypass-Cache` 专用于重生成）；**成本 JSONL 强制字段**按日轮转；可重试/不可重试显式分类（`RETRYABLE={429,500,502,503,504}`，401/403/404/413/422 立即换）；**SecretLoader 启动校验 + 拦截占位值 `change-me`/`your-key-here` + fail fast**；受控词表（camera 10 / emotion 12 / transition 5 硬性枚举） |

**落地方式**：新建 `vm/audit/` 纯 Python 包（无 I/O、可单测）。**这一步不依赖 GPU，可以立刻做。**

---

## C 档 · 连续性架构（提升成品率）

| # | 项 | 说明 |
| --- | --- | --- |
| **C1** | **装 [`ComfyUI-H3-Motion-Context`](https://github.com/NikoDemon80/ComfyUI-H3-Motion-Context)（⭐1006，GPL-3.0）** | **从 latent 层**把上一段 H3 的**尾帧 + 音频**接到下一段，避开"解码→缩放→重编码"的**色偏与糊化**；音频锚定窗口"**以接缝为终点向前回看**"实现真正续播；带 **Seam Probe** 判断真延续还是模仿。**比"上段末帧→下段首帧"高一档**。⚠️ GPL-3.0：**只装节点用，不抄代码进我们仓库** |
| **C2** | **`lines[]` 镜内台词轨**（取代 `dialogue` + `narration`） | **推翻我"52 镜是结构下限"的结论** —— 那是**我们 schema 的限制**，不是 H3 的限制。一镜可承载多句对白 `{at, dur, speaker, text}` → 短镜可合并。`narrator` 是保留字不是角色 |
| **C3** | **`units[]` 聚合** | H3 单次 4-15s，**3 秒镜和 15 秒镜显存占用相同**。按"相邻且同 scene_id"确定性分组，2-3 个短镜合并成**一次 9-12s 请求，段内切镜由模型完成** → 52 次请求 → 约 20 次，**且比逐镜硬拼更连续**。校验必须"按原顺序完整覆盖、不漏不重不调序"；共享负面**不能各镜求并集** |
| **C4** | **`tail_context` 尾帧续接（不需换节点）** | ffmpeg 截末帧 → 视觉模型出描述文本 → 注入下一镜提示词。**用现有能力就能做，风险远低于换节点** |
| **C5** | **H3 起始音节杂音裁剪** | 每段开头 **0.12-0.16s** 的"起始音节"杂音自动裁掉。具体、可立即验证 |
| **C6** | **本机长片能力**（`/object_info` 已确认存在） | `LongVideoOrchestratorT8`（固定窗口时间轴 + 每段不同提示词 + 从未接受的 manifest 段续跑 + 防止意外多生成一段）、`NativeLatentCheckpointSave/Load`（**latent 级检查点**）、`LongVideoSeamDriftT8`（接缝漂移报告 + 有界增益修复，不安全帧回滚源）、`VoiceProfileT8` + `VoiceLibrarySave/Load`（**角色音色锁定**）、`DialogueBoundaryAnalyzerT8`（本地 whisper 台词边界） |
| **C7** | **帧数上限可放宽** | 实测合法上限 **498 帧 = 20.75s**，我们现限 362（15.08s） |
| **C8** | **`camera` 拆枚举 + 补 `angle`** | 我们的 `camera` 现在是自由文本 `"缓推（Push In, small amplitude, slow speed）"` —— **英文渲染参数泄漏进中文枚举位** |

---

## D 档 · UI 重构（P3，对应你"UI 简陋"的抱怨）

**printfilm 的分集台是四栏 `168px | 300px | 1fr | 420px` + 底部胶片条，与我们的三栏同构，栏宽可直接取。**

| # | 项 | 要点 |
| --- | --- | --- |
| **D1** | **派生式镜头状态纯函数 + ★加 `stale` 档** | printfilm 的 `shotDisplayKind()` 是"**不信任数据库状态字段，按素材完备度推导**"。**但它缺 `stale`（需重渲）这一档 —— 而我们有指纹，正好补上这个差异化** |
| **D2** | **表格化清单 > 卡片网格** | 7 列固定宽度，行 hover 淡青柠。**卡片网格只适合窄屏，信息密度远不如表格** |
| **D3** | **"一眼看出状态"的三重表达** | ① 缩略图格子里直接写「待出图/生成中」② 状态列三色（完成 `#3d6500`+16px lime ✓ / 警告 `#8a5a12` / 失败 `#a1261a`）③ **操作按钮随状态改名**（`生成画面↔重绘画面`），disabled 用 `title` 说明原因 |
| **D4** | **聚焦式编辑弹层 + 每字段"改了会影响什么"** | 点单元格 → 弹层 + 三聚焦模式；**每个字段下一行 12px 灰字写清下游影响**（「改完需重新生成配音/成片」）—— 这是"改完提示重渲"的最低成本答案 |
| **D5** | **右下角全局队列 FAB** | badge 计数、`排队 #N` FIFO 序号、失败就地展开"标题+原因+建议"、新任务自动展开、进行中用**不确定态动画条而非假百分比**。**调研者评为本轮性价比最高的单个模块** |
| **D6** | **分段播放器**（成片预览） | 单 video 顺序播 → 播完自动跳下一镜 → 胶片条与中栏跟随；**进度条按每镜时长等比例分段**（`flex: segment.durationSec`）→ **既是进度条、又是时长分布、又是导航** |
| **D7** | **逐镜质检可视化 + 筛选/搜索/多选** | **三个 UI 项目全都没有** → 必须自建，也正是超越点。建议：状态列多一档 `qc_failed` + 缩略图角标 + 右侧质检 tab + **用分段进度条的段底色表达绿/黄/红** |
| **D8** | **视觉数值直接抄** | `--pf-lime:#b6ff00; --pf-ink:#111318; --pf-bg:#f7f8fa; --pf-card:#fff; --pf-radius:14px/10px`；统一焦点环 `border-color:color-mix(lime 70%,ink)` + `box-shadow:0 0 0 3px color-mix(lime 28%,transparent)`；弹层 `width:min(560px,100%); radius:18px; animation:panel-in .28s cubic-bezier(.22,1,.36,1)`；数字一律 `tabular-nums` |
| **D9** | **★ 必须补的坑** | 键盘处理要排除 `contentEditable`（milimo 只挡了 `INPUT`/`TEXTAREA`）——**我们内联编辑台词时必须补 `isContentEditable`**，否则打字会触发快捷键 |

---

## E 档 · 工程规范（低成本高收益）

| # | 项 | 要点 |
| --- | --- | --- |
| **E1** | **资产 lock → 409 拒改 → 需显式解锁** | 比我们现在"任何产物都能被覆盖"安全得多。`selected`/`favorite`/`locked` 是**三个独立布尔位**（不是互斥状态）；`variation_of` 自引用外键做候选图谱系；`locked_at`/`locked_by` 记录谁何时锁的；rollback API |
| **E2** | **状态 key 带源码/契约哈希 → 更新即作废，零迁移代码** | 加 `schema_version` + `prompt_version` + `script_rev`（章节内容指纹）；控制台前端状态 key 用 `vm/shots.py` 内容哈希 |
| **E3** | **预算护栏：暂停等人批，而非硬失败** | 「一次『一键成片』内部连着发几十次付费调用，**中途没有任何刹车点**」。**一条该贴墙上的原则：「一道通向不了人的告警，和没有告警是一回事。」** → `pause` 必须带出「已花多少 / 卡在哪步 / **再放行多少才能继续**」（对我们换成"预计还需 N 分钟 GPU / N 次 H3 调用"） |
| **E4** | **CI 门禁：契约变更必须同步消费方** | wind-comic 的 `gate:consumer`「上线即抓到 2 个人肉复检漏掉的真 SSRF」 |
| **E5** | **工程哲学（该贴 CI 墙上）** | 「抽成纯函数不是为了复用，是为了**可测**：测试只能断言源码里出现过某个字符串 —— 把判断条件改成 `false` 那条断言依然绿。**锁写法不锁行为，等于没锁。**」→ 我们的机械质检若只断言"某函数被调用过"而非"给定输入产出正确判定"，就是同一个病 |

---

## F 档 · 明确不做（三份调研的负面清单汇总）

| 反对项 | 替代 / 理由 |
| --- | --- |
| **通用 DAG 平台当运行时**（Airflow/Dagster/Prefect/Flyte/Temporal） | **只抄概念**：SQLite 一张 `stage_state` + 文件哈希增量 + 写 `.tmp` 原子改名，**<200 行**实现其核心语义。我们已经有了 |
| **自研完整 NLE** | 改为**导出剪映草稿** |
| **Electron 桌面壳 + 灵动岛 + 风扇超频** | NiliX 的**提示词纪律与审片闭环**要抄，**外壳不要**（我们已砍掉 ✅） |
| Postgres + Redis + BullMQ + MinIO 全家桶 | 单机 **SQLite + 文件系统**更好调试 |
| 3D 白模 / Blender 预演 | 成本高、收益低（Slate 的做法也不适用） |
| 数字人 / 口型同步 / 换脸 | 超出范围 |
| 多租户 / 积分计费 / 支付 / 管理后台 | 单人自用 |
| 分布式渲染农场 / LAN 编排 | 单卡 |
| 无限画布 / React Flow 节点编辑器 | 与"零框架零构建"冲突，且与一维分镜顺序冲突 |
| 云同步 / 团队协作 / 评论审批 / 登录鉴权 | 单人自用 |
| i18n / 移动端断点 | **只保留 `aria-live` 与 `role=dialog/tablist/listbox` 这类低成本无障碍** |
| 队列去重 + 项目互斥锁 + 用户级并发限额 | 前端 disable + 后端一把锁足矣（我们已有） |
| KPI 仪表盘 | printfilm 自己的 Negative 清单就禁止首屏 KPI 条 |
| 模板商店 / 社区 / 平台分发 / 分享链接 | 不是我们的目标 |
| **storyforge 的摘要式链路** | **架构级错误**：原文第 1 步就丢，对白必丢 |
| **storyforge 的"多角色同框 = 每角色一个视频任务"** | **概念上就是错的**，2 人对话产出 2 段互不相干视频且无合并逻辑 |
| **非阻塞质检** | `scene_agent.py:242-248` 发现 issues **只写 log 然后原样返回** —— **"装了但没通电"**。要求：每个判定必须**阻塞 / 触发重生 / 写进 manifest 且 UI 可见**三选一 |
| **声明向量去重却传全零向量** | 「文档承诺 > 代码实现」典型。→ **未接线的能力必须显式标注** |
| **内存检查点**（`MemorySaver()`） | 进程重启即全丢，与"Resumable"宣称矛盾 |
| **超时也走完整退避重试** | 本地渲染超时应**直接失败 + 保留检查点**，重试只留给明确瞬时错误 |
| **巨型单文件架构** | wind-comic orchestrator **254KB**（自陈 5471 行/113 处 any，6 处在**公开方法签名**上——"**重构无从谈起**"）。学 storyforge 的纪律：227 文件、最长 39KB、`docs/RULES.md` 强制"prompt 全外置" |
| **双 orchestrator 并存** | 演示版文件名反而更"正"。**「主路径修好了、旁路没跟上」是最隐蔽的 bug 类别之一** |
| **自我推销式 README** | 数字无法验证、时效性极强、**误导阅读者把宣传当能力**（"8-agent pipeline"实际是同一 LLM 的 8 段 system prompt） |

---

## 建议的施工顺序

| 阶段 | 内容 | 依赖 | 预估 |
| --- | --- | --- | --- |
| **第 1 步** | **A 档全部 8 项**（正确性隐患） | 无 | 小 |
| **第 2 步** | **B 档审计层**（`vm/audit/` 纯 Python 包） | 无（不依赖 GPU） | 中 |
| **第 3 步** | **D 档 UI 重构**（printfilm 四栏 + 我们的 `stale` 档 + 逐镜质检可视化） | B 档的审计结果作为展示内容 | 中大 |
| **第 4 步** | **C1 装 Motion-Context 节点 + C4 尾帧续接**，做 3 镜对照实验 | 需 GPU | 中 |
| **第 5 步** | **C2 `lines[]` + C3 `units[]` 聚合**（schema v2 主力） | C1/C4 的实验结论 | 大 |
| **第 6 步** | **E 档工程规范**（lock/状态 key/预算护栏/CI 门禁） | 可穿插 | 小 |

---

## 需要你拍板的点

1. **A 档（8 项正确性隐患）现在直接修，还是先等你看完完整成片？**
2. **B 档审计层**是我建议的最高优先级（不烧 GPU、直击废片率）。**先做哪几个？**（我建议 B1 对话覆盖度 + B2 节奏形状 + B5 对白零丢失加固）
3. **C 档连续性**：先做**低风险的 C4（尾帧描述注入）+ C5（起始音节裁剪）**，还是直接**装 C1 节点 + 做 3 镜对照实验**？
4. **C3 `units[]` 聚合**是架构级改动（分镜表 schema v2）。**等你看到成片、确认连续性确实是最大问题之后再做？**
5. **D 档 UI**：现在就重构（printfilm 四栏），还是等 B 档审计结果出来一起做（这样质检可视化一次成型）？
6. **E1 资产锁定语义**（lock → 409 拒改）：要不要现在加？我们现在**任何产物都能被覆盖**。

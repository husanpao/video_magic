# 控制台完善计划（评估 + 设计）

> 2026-09-23 · 基于用户反馈："UI 比较简陋 / 每章换衣服怎么办 / 拆镜后不知道每镜内容也不能改"
> 本文所有"现状"结论都有代码或实测依据，逐条标了出处。

---

## 第一部分 · 现状评估

### 1.1 服装：按章节换装**做不到**（三重锁 + 一处架构错位）

| # | 锁 | 位置 | 实际内容 |
| --- | --- | --- | --- |
| ① | 图锁 | `refs/char_<名>.png` | 定妆照由 `prompts/char_<名>.txt` 生成，服装已经**烤进参考图** |
| ② | 文锁 | 每镜 `subject_definitions` | 逐字抄角色卡的服装句。实测 `1-1-01`：`He wears a moon-white coarse cotton monk robe with an ochre-red kasaya...` |
| ③ | 明令锁 | 每镜 `retention_analysis` | `clothing must match <Picture 1> exactly` / `do not change the clothing colour` |

**架构错位**：`vm/plan.py:621 write_char_prompts()` 写的是 `prompts/char_<名>.txt`，
**每个角色在整个项目里只有一份**。第二章换装要么被忽略，要么把第一章的卡改掉。

**突破口**（本项目 A/B 实测结论）：**提示词里的文字描述会压过参考图**
（男性参考图 + 女性文字 → 输出女性，像素差 34.13）。所以"图锁身份 + 文控服装"是可行的，
**但当前代码用 ③ 明令禁止了它**。

### 1.2 镜头内容：看不到、改不了

- UI 每张镜头卡只渲染 6 个字段（`vm/static/index.html:408-414`）：
  `id`、状态标签、`sec`、`chars`、`shot_size`、`camera`、QC 标签
- **不显示**：`dialogue`、`narration`、`action`、**六段式提示词全文**（实测单镜 3200 字）
- **没有任何编辑接口**（`vm/web.py` 现有 12 个接口全是读/跑/停/重渲/抽卡）

### 1.3 UI 简陋的具体表现

| 问题 | 说明 |
| --- | --- |
| 无镜头详情 | 只有缩略图 + 6 个字段，看不到"这镜在演什么" |
| 无编辑 | 台词写错了、运镜不满意，只能手改 JSON 文件再重跑 |
| 无筛选/搜索 | 52 个镜头平铺，想找"质检不合格的"只能一个个看 |
| 信息密度低 | 卡片式网格，一屏放不下几个镜头，看不出全片节奏 |
| 角色区与镜头区割裂 | 抽卡/采纳在下面，看不到"这个角色的定妆照影响哪些镜头" |
| 无成片视图 | 没有一键预览+下载+分镜对照 |
| 无单镜重写 | 某一镜提示词写歪了，只能重跑整章拆镜（3 分钟 + 95k tokens） |

---

## 第二部分 · 方案设计

### 2.1 服装系统（**推荐方案 C：图锁身份 + 文控服装 + 可选换装定妆**）

**数据模型**：项目级 `costumes.json`

```json
{
  "孙悟空": [
    {"id": "default", "label": "常服",
     "prompt": "a red coarse cotton short tunic with a red cloth waist sash and a plain wooden bead necklace",
     "ref": "char_孙悟空.png"},
    {"id": "armor", "label": "战甲",
     "prompt": "golden chainmail armour over a red under-robe, a golden fillet on the brow",
     "ref": "char_孙悟空@armor.png"}
  ]
}
```

- `prompt` = 服装文字描述（进 `subject_definitions`）
- `ref` = 该服装对应的参考图；**可以缺省**（缺省 = 复用默认参考图，即"图锁身份 + 文控服装"）
- 需要更稳时可以给变体**抽一套定妆照**（复用现有抽卡机制）

**镜头表新增可选字段** `costume`：

```json
{"id": "2-3-01", "sec": 6, "chars": ["孙悟空"], "costume": "孙悟空=armor", "seed": 3100, "prompt": "..."}
```

**三处改动**：

| 文件 | 改动 |
| --- | --- |
| `vm/plan.py` | ① 拆镜时按章节/场次写入 `costume`；② `subject_definitions` 用该服装的 `prompt`；③ **`retention_analysis` 措辞按是否有变体分流**：无变体保持"服装必须与图一致"（现状，最稳）；有变体改成"**面部/发型/年龄/性别/体型必须与 `<Picture 1>` 一致；服装以本镜 `subject_definitions` 的文字为准**" |
| `vm/gen.py` | 挂载参考图时按 `(角色, 服装)` 取 `ref`，而不是固定 `char_<名>.png` |
| `vm/chars.py` | 支持"给某角色的某服装变体抽卡"，产物落 `_gacha/<名>@<服装>/` |

**指纹自动生效**：`shot_fingerprint` 已含参考图的 `名@mtime纳秒:size`。
换装改的是 `ref` → 指纹变 → 相关镜头**自动判 stale 自动重渲**，不需要额外开发。

**默认行为不变**：没有 `costumes.json` 或角色只有一个变体时，一切同现在。

### 2.2 镜头详情与编辑

**新增接口**（沿用现有 REST 风格）

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| GET | `/api/shot?project=&id=` | 单镜完整详情（六段式全文、action、台词、seed、帧数、指纹、QC 指标、同角色其他镜头） |
| POST | `/api/shot/update` | 改字段：`dialogue/narration/camera/shot_size/sec/chars/costume/prompt` |
| POST | `/api/shot/rewrite` | **只重写这一镜的六段式提示词**（不重跑整章，省 token） |
| POST | `/api/shot/split` | 按标点/指定位置拆成两镜（复用已实现的 `_split_by_punct`，保证不丢字） |
| POST | `/api/shot/merge` | 合并相邻两镜 |
| POST | `/api/shot/insert` / `delete` | 增删镜头 |
| POST | `/api/shots/renumber` | 重排镜头号 |

**关键约束**：
- 改完必须让指纹失效 → 相关镜头变 `stale`（这是现有机制，天然满足）
- **编辑动作本身不碰 ComfyUI**（纯写盘），只标 stale，由用户决定何时重渲
- 所有写盘原子化；每次编辑前留一份 `shots/*.json.bak`（可撤销）
- 编辑后**必须重跑 `validate_shots`**并在响应里返回问题清单（不能让用户存进一个坏表）

### 2.3 UI 重构：三栏工作台

```
┌───────────────────────────────────────────────────────────────────────────┐
│  西游记 ▾   [⓪拆镜] [①定妆] [②渲染] [③质检] [④合成]    ▓▓▓▓░░░░ 7/52 [停止] │
├──────────────┬──────────────────────────────────────┬─────────────────────┤
│ 镜头 52      │  1-7-01   中景 · 缓推 · 6s · 孙悟空   │  实时日志            │
│ [全部][缺失] │ ┌──────────────────────────────────┐ │  [21:31] 提交 1-7-01 │
│ [需重渲][就绪]│ │   视频预览 / 缩略图（点击播放）    │ │  [21:32] ✅ 1.8MB    │
│ [不合格]     │ └──────────────────────────────────┘ │                     │
│              │  台词  「…」        [编辑] [重渲]      │  ── 角色定妆 ──      │
│ ▣ 1-1-01 ✓  │  旁白  「…」                          │  孙悟空  7 张候选 [抽]│
│ ▣ 1-2-01 ✓  │  ── subject_definitions ──────────┐  │  唐僧    6 张候选 [抽]│
│ ▣ 1-3-01 ✓  │  │ <Subject 1> = …                 │  │  沙僧    2 张候选 [抽]│
│ □ 1-4-01    │  ├─ summary ───────────────────────┤  │                     │
│ □ 1-4-02    │  ├─ retention_analysis ────────────┤  │  ── 成片 ──          │
│ …            │  ├─ detailed_description ─────────┤  │  EP01.mp4 [播放][下载]│
│              │  ├─ overall_soundscape ───────────┤  │  质检 通过12 可疑3     │
│ 时间轴缩略图  │  └─ non_diegetic_music ───────────┘  │                     │
│ ▭▭▭▭▭▭▭▭▭▭  │  [+ 插入] [拆分] [合并] [删除]         │                     │
└──────────────┴──────────────────────────────────────┴─────────────────────┘
```

**设计要点**：
1. **左栏镜头列表**：紧凑行（缩略图小图 + 编号 + 状态点 + 时长），带筛选标签；底部一条**全片时间轴**（色块 = 状态，一眼看出哪段没渲）
2. **中栏镜头详情**：预览 → 台词/旁白（行内可编辑）→ 六段式**分节可折叠编辑** → 底部操作（插入/拆分/合并/删除/重渲/重写提示词）
3. **右栏**：日志流 + 角色定妆（候选缩略图直接内联，不用跳转）+ 成片与质检汇总
4. **零框架零构建**保持不变（原生 JS + CSS），但引入设计变量：统一间距/圆角/阴影/状态色

---

## 第三部分 · 实施分期

| 期 | 内容 | 验收（我能演示） |
| --- | --- | --- |
| **P1** | **镜头详情 + 编辑**（最痛，先做） | 点任一镜头看到六段式全文与台词；改台词→保存→该镜变 `stale`；改回→仍 stale；重跑校验有错会拒绝保存 |
| **P2** | **服装变体系统** | 给孙悟空加"战甲"变体（只写文字）→ 第 2 章相关镜头提示词里服装文字变、`retention_analysis` 措辞自动放宽 → 渲染出的衣服真的换了（抽 1 镜验证） |
| **P3** | **UI 重构为三栏工作台** | 新布局可看/可点；筛选、时间轴、内联编辑、成片预览全部可用 |
| **P4** | **单镜重写提示词** | 1-7-01 提示词改歪 → 点"重写"→ 只调 1 次 LLM（不是 53 次）→ 该镜提示词更新且指纹失效 |
| **P5** | **质检与成片视图** | 质检结果按镜聚合（通过/可疑/不合格），可疑镜头能一键跳转；成片可播放下载 |

**每期做完停下来给你验**，不闷头往下做。

---

## 第四部分 · 需要你拍板的点

1. **换装场景确认**：你说的"每章换衣服"是**同一个角色在不同章节穿不同衣服**，还是**同一章里不同场次**也要换？后者对提示词写法要求更高（同一镜内不能换）。
2. **换装实现深度**：
   - 只做"图锁身份 + 文控服装"（零额外抽卡，服装细节一致性略弱）
   - 还是也支持"给每套服装抽定妆照"（更稳，但每套要抽 2 张）
3. **服装配置的编辑入口**：放控制台里（可视化增删改）还是先手写 `costumes.json`？
4. **P1 优先做还是 P3 优先**：我建议 **P1 先行**（先让你能看懂并改镜头），P3 再整体换皮。
5. **成片预览**：需要在线播放 + 分镜对照（左边成片右边对应镜头）吗？

---

## 第五部分 · 决策已定（2026-09-23）

| 问题 | 决定 |
| --- | --- |
| 换装场景 | **三种都可能**（按章 / 按场次 / 按单镜） |
| 换装做法 | **只做「图锁身份 + 文控服装」**（不额外抽换装定妆照） |
| 服装配置入口 | **控制台里可视化增删改** |
| 分期顺序 | **P1 镜头详情+编辑 先行**，P3 UI 重构随后 |
| 成片预览 | **要播放下载 + 分镜对照** |

### 5.1 服装的最终数据模型（按"三种都可能"推导）

因为要支持到**单镜级**，服装必须挂在镜头上，并按级回落：

```
镜头级 shot.costume[角色]  →  场次级 scene_costs[场次][角色]  →  角色默认  →  无（用原卡服装句）
```

- `costumes.json`（项目级）：只定义**这个角色有哪些服装可选**
  ```json
  {"孙悟空": [
     {"id":"default","label":"常服","prompt":"a red coarse cotton short tunic ..."},
     {"id":"armor",  "label":"战甲","prompt":"golden chainmail armour over a red under-robe ..."}
  ]}
  ```
  **只有 `prompt`（文字），没有 `ref`** —— 参考图始终是 `refs/char_<名>.png` 那一张，
  它只负责脸/发型/体型/年龄/性别。
- `shots/*.json` 的镜头级字段：`"costume": {"孙悟空": "armor"}`
- 场次级：`"scene_costs": {"3": {"孙悟空": "armor"}}` 放在 `project.json`（或 shots 文件的元数据里）

### 5.2 提示词生成规则（P2 落地）

| 情况 | `subject_definitions` 里的服装句 | `retention_analysis` 措辞 |
| --- | --- | --- |
| 角色**只有一套**服装（现状） | 用角色卡的服装句 | 保持现状：`clothing must match <Picture 1> exactly`（最稳） |
| 角色**有多套**且本镜指定了变体 | 用**该变体**的 `prompt` | 放宽为：`facial features, hairstyle, age, gender and body build must match <Picture 1> exactly; the CLOTHING is authoritative from the wording above and MUST follow it, even where it differs from <Picture 1>` |

**依据**：本项目 A/B 实测——文字描述会压过参考图（男图 + 女文 → 出女性，像素差 34.13）。
所以要换装就必须**显式声明服装以文字为准**，否则模型会跟着图走。

### 5.3 UI 里的批量换装（覆盖"按章/按场次"两种场景）

因为痛点主要是"整章/整场次换"，所以编辑界面必须有**批量操作**：

```
镜头列表上方： [批量] 选 → 「第 2 章」或「第 3 场」或「选中 N 个镜头」
              → 设服装：孙悟空 = 战甲 / 唐僧 = 常服 → 应用
```
- 批量改动同样只标 `stale`，不自动重渲
- 界面上要能一眼看出"哪些镜头用了非默认服装"（镜头行加服装标签）

### 5.4 跨章角色卡的架构错位（P2 必须一并修）

现状 `prompts/char_<角色>.txt` 是整项目一份。有两种处理：
- **保持一份，但服装句抽出来**到 `costumes.json`（推荐，改动小、语义更清晰：卡管外貌，服装表管服装）
- 或按章拆分 `prompts/<章>/char_<角色>.txt`（改动大，且同一角色跨章的**脸**本来就不该变）

**选前者**：`char_<名>.txt` 只留外貌（去掉服装句），服装全部走 `costumes.json`。
